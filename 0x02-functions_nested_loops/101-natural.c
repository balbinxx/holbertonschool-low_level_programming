#include <stdio.h>
/**
 * main - adds multiples
 * Return: void
 */
int main(void)
{
	int x = 1;
	int y = 0;

	while (x < 1024)
	{
		if (x % 3 == 0)
			y += i;
		else if (x % 5 == 0)
			y += i;
		x++;
	}
	printf("%d\n", y);

	return (0);
}
